# Real node_modules is required for Expo / Metro. In-repo this is inherited
# from the library root; keep a copy so the example still works if copied out.
nodeLinker: node-modules

# Yarn 4.18 defaults this to false. patch-package needs the postinstall script.
enableScripts: true

npmMinimalAgeGate: 0
