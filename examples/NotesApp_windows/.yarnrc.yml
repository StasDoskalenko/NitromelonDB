# Real node_modules is required for Metro / RNW. In-repo this is inherited
# from the library root; keep a copy so the example still works if copied out.
nodeLinker: node-modules

enableScripts: true

approvedGitRepositories:
  - https://github.com/Nozbe/cavy.git
  - https://github.com/radex/big-list-of-naughty-strings.git

npmMinimalAgeGate: 0
